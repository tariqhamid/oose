language: node_js
sudo: false
node_js:
  - "5.0"
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
services:
  - couchdb
  - redis
env:
  - TRAVIS=travis CXX=g++-4.8
before_script:
  - curl -X PUT localhost:5984/oose
  - curl -X PUT localhost:5984/_config/admins/root -d '"blah1234"'